services:
  ans-agent:
    build:
      context: ..
      dockerfile: deploy/Dockerfile
    restart: unless-stopped
    env_file:
      - ../.env
    volumes:
      - ../data:/app/data
      - ../workspace:/app/workspace
    ports:
      - "127.0.0.1:8000:8000"
